{% extends 'base.html.twig' %}

{% block title %}Jeu de carte{% endblock %}

{% block body %}
    <div class="maincontainer">
        <div class="col-md-10 offset-md-1">
            <h2 class="text-center card-title">&hearts;&spades; Card Game  &diams;&clubs;</h2>
            <div class="form-group">
                <div class="col-md-6 offset-md-3">
                    <a href="{{path('filter_home')}}" class="btn btn-primary">Filtrer par Couleurs</a>
                    <a href="{{path('reset_home')}}" class="btn btn-danger">Tirer un nouveau jeu de cartes</a>
                </div>
            </div>
            <div class="row">
                {% set hands = app.session.get('PlayerHand') %}
                    {#{% switch hand %}
                        {% case 11 %} 
                            {{ "%hand["valeur"]%" | replace({'%hand["valeur"]%': "Valet"}) }}
                        {% case 12 %}
                        {% case 13 %}
                        {% case 14 %}
                    {% endswitch %}#}
                {% for hand in hands %}
                    <div class="col-md-2 item-card">
                        <div class="span-left">{{hand["valeur"]}}</div>
                        <div class="span-center">{{hand["couleur"]}}</div>
                        <div class="span-right">{{hand["valeur"]}}</div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}